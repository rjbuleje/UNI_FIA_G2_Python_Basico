def factorial(x):
    if x == 0 or x == 1:
        print("Hola  " + str(10-x))
        return 1
    else:
        print("Hola" + str(x) ) 
        return x * factorial(x-1)
        
    
def


def run():    
    num = input("Ingresa un entero al cual quieras calcular su factorial: ")
    num = int(num)
    resultado = factorial(num)
    print(resultado)
    h' ff
    derivva (f,h=


if __name__ == "__main__":
    run()